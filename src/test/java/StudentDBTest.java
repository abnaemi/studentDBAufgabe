import static org.junit.jupiter.api.Assertions.*;

class StudentDBTest {

    @org.junit.jupiter.api.Test
    void shouldReturnAllNames() {
        //GIVEN
        StudentDB firstGroup = new StudentDB(new String[]{"Tai", "Kai", "Mari"});


        //WHEN
        StudentDB actual = new StudentDB(new String[]{"Tai", "Kai", "Mari"});
        //THEN

    }

    @org.junit.jupiter.api.Test
    void testToString() {

        //GIVEN
        StudentDB firstGroup = new StudentDB(new String[]{"Tai", "Kai", "Mari"});

        //WHEN
        String actual =
        //THEN
        assertEq

    }
}